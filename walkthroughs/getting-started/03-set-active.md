## Set the Active Database

Before running a query, tell the extension which database to use:

- **Right-click** the database node in the tree and choose **Set Active**, or
- Click the **Firebird** indicator in the VS Code status bar (bottom-left) and pick a database from the list.

The active database's name appears in the status bar once set.

[Set Active Database](command:firebird.setActive)
